namespace Statix.Sitemap
{
    // sitemaps.org/protocol.html
    public enum ChangeFreq
    {
        Unknown,
        Always,
        Hourly,
        Daily,
        Weekly,
        Monthly,
        Yearly,
        Never,
    }
}
